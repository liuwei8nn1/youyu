package com.youyu.gateway.config;

import org.springframework.context.annotation.Configuration;

/**
 * 网关配置类
 * 
 * 注意: 路由配置已移至 application.yml,此处仅保留必要的 Bean 配置
 */
@Configuration
public class GatewayConfig {
    // 路由配置已在 application.yml 中定义
    // 如需动态路由或复杂逻辑,可在此扩展
}
